This project calculates the rating of Dota heroes (AI) based on game statistics
(statistics are available for download).
The rating calculation formula itself is semi-empirical.
Project is not finished, the frontend part and separation by attributes is in progress.
Manage.py commands for download:
-loadheroes (heroes)
-loadappearances(appearances)
